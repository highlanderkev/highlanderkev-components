import { LitElement, html, customElement, property } from 'lit-element';
import { TemplateResult } from 'lit-html';

@customElement('kpw-header')
export class HeaderComponent extends LitElement {
  public render(): TemplateResult {
    return html``;
  }
}
